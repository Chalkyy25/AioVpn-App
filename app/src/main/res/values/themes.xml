<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.AioVpn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/aio_background</item>
        <item name="android:windowBackground">@color/aio_background</item>
        <item name="android:statusBarColor">@color/aio_background</item>
        <item name="materialAlertDialogTheme">@style/Aio.Dialog</item>
    </style>
</resources>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Fire TV / Android TV support: require Leanback for TV-only behavior -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:banner="@drawable/banner"
        android:icon="@mipmap/ic_launcher"
        android:logo="@drawable/banner"
        android:label="@string/aio_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AioVpn">

        <activity
            android:name="uk.co.aiovpn.android.auth.SplashActivity"
            android:banner="@drawable/banner"
            android:logo="@drawable/banner"
            android:exported="true"
            android:theme="@style/Theme.AioVpn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <!--
                Adding LAUNCHER back as a fallback,
                though LEANBACK_LAUNCHER is primary for Fire TV
                -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="uk.co.aiovpn.android.auth.AuthGateActivity"
            android:exported="true"
            android:theme="@style/Theme.AioVpn" />

        <activity
            android:name="uk.co.aiovpn.android.login.LoginActivity"
            android:exported="false" />

        <activity
            android:name="uk.co.aiovpn.android.home.HomeActivity"
            android:exported="false" />

        <activity
            android:name="uk.co.aiovpn.android.login.ServerListActivity"
            android:exported="false" />

        <activity
            android:name="uk.co.aiovpn.android.home.AccountActivity"
            android:exported="false" />

        <activity
            android:name="uk.co.aiovpn.android.home.SettingsActivity"
            android:exported="false" />

        <activity
            android:name="uk.co.aiovpn.android.home.SplitTunnelingActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
